import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("home"),
    );
  }
}

// TODO(me): so the plan for this screen is for it to be a pinned page
// You pin items from the other pages and they end up here in a list or grid
// or something for quick access.
